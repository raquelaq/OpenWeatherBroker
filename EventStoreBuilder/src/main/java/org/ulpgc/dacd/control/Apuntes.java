package org.ulpgc.dacd.control;

public class Apuntes {
    // Crear una clase que me cree el directorio (el que está en la descripción del trabajo)
    // Crear otra clase que coja los mensajes con textMessage.getText() (los eventos pasados a String) y los almacene en el directorio
    // es mejor idea escribirlo directamente en el directorio

    // Crear otra clase que cree el directory eventstore/prediction.Weather/{ss}/{YYYYMMDD}.events (el unico que debe cambiar es YYYYMMDD
    // Llamar a ss open-weather
}
